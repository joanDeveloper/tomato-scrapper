import { ServiceBroker } from "moleculer"

export const Query = {
    readUser: async (root: any, args: any, ctx: any, info: any) => {
        const broker: ServiceBroker = ctx.broker
               
    }
}