import { ServiceBroker } from "moleculer"

export const Mutation = {
    verifyToken: async (root: any, args: any, ctx: any, info: any) => {
        const broker: ServiceBroker = ctx.broker
        
    }
}